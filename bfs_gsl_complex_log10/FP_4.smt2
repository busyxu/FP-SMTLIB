(declare-fun y_ack!84 () (_ BitVec 64))
(declare-fun x_ack!85 () (_ BitVec 64))
(assert (fp.geq (fp.abs ((_ to_fp 11 53) x_ack!85)) (fp.abs ((_ to_fp 11 53) y_ack!84))))
(assert (let ((a!1 (and (fp.eq (fp.abs ((_ to_fp 11 53) y_ack!84))
                       ((_ to_fp 11 53) #x0000000000000000))
                (fp.eq (fp.abs ((_ to_fp 11 53) x_ack!85))
                       ((_ to_fp 11 53) #x0000000000000000)))))
  (not a!1)))

(check-sat)
(exit)
